import http from './http';

//getUsers

export const getUsers = () => {
    return (dispatch, getState) => {
        //TODO request con axios
        console.log("Se llamo a la accion getUsers");
    }
}